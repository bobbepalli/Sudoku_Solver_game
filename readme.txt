This directory contains two python files and one image:
1. GUI.py
2. solver.py
3. sudoku1.png

To run the game simply run the GUI.py file from IDLE or the command line.

Note: You require pygame to play this game, pygame is a module. Before running this game we have to install pygame in system.
